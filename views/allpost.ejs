<!DOCTYPE html>
<html data-theme="halloween">
  <head>
    <title>allpost</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/daisyui@2.19.1/dist/full.css"
      rel="stylesheet"
      type="text/css"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <% include ./partials/navbar %>
    <div class="text-center text-info ">
      <h1>ALL POST</h1>
    </div>
    <% for(var i=0; i< posts.length; i++) {%>
    <div class="card w-80 glass shadow-2xl inline-block m-5 " style="height: 510px;" >
      <figure>
        <img src="<%= posts[i].image %>" alt="<%= posts[i].title %>" class="h-45"/>
      </figure>
      <div class="card-body">
        <h2 class="card-title"><%= posts[i].author.name %> </h2>
        <h4 class="text-lg"><%= posts[i].title %></h4>
        <p class="text-sm"><%= posts[i].content %></p>
        <div class="card-actions justify-end">
          <a
            href="/postdetails/<%= posts[i]._id%>"
            class="btn btn-outline btn-primary"
            >DETAIlS
          </a>
        </div>
      </div>
    </div>
    <%}%>
  </body>
</html>
